# README patch note (Jobs Apps Script)

If you want to mention the Apps Script in the repo README without rewriting the whole file,
you can add a small note near the jobs data section.

Suggested text:

- `data/jobs.json`
  - Generated from a Google Sheet.
  - The sheet uses a Google Apps Script layer to approve/reject submissions (Status dropdown)
    and to copy approved rows into `For Show`.
  - Maintainer doc: `docs/jobs-pipeline.md`
